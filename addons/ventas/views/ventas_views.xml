<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_venta_tree" model="ir.ui.view">
        <field name="name">iv.venta.tree</field>
        <field name="model">iv.venta</field>
        <field name="arch" type="xml">
            <tree string="Ventas" decoration-success="estado == 'confirmada'" decoration-muted="estado == 'cancelada'">
                <field name="name"/>
                <field name="fecha"/>
                <field name="cliente_id"/>
                <field name="subtotal"/>
                <field name="impuesto"/>
                <field name="total"/>
                <field name="estado"/>
            </tree>
        </field>
    </record>

    <record id="view_venta_form" model="ir.ui.view">
        <field name="name">iv.venta.form</field>
        <field name="model">iv.venta</field>
        <field name="arch" type="xml">
            <form string="Venta">
                <header>
                    <button name="action_confirmar" string="Confirmar" type="object"
                            class="btn-primary" invisible="estado != 'borrador'"/>
                    <button name="action_cancelar" string="Cancelar" type="object"
                            invisible="estado == 'cancelada'"/>
                    <field name="estado" widget="statusbar" statusbar_visible="borrador,confirmada,cancelada"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="cliente_id"/>
                        </group>
                        <group>
                            <field name="fecha"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Productos">
                            <field name="detalle_ids">
                                <tree editable="bottom">
                                    <field name="producto_id"/>
                                    <field name="cantidad"/>
                                    <field name="precio_unitario"/>
                                    <field name="subtotal" readonly="1"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                    <group class="oe_subtotal_footer">
                        <field name="subtotal"/>
                        <field name="impuesto"/>
                        <field name="total" class="oe_subtotal_footer_separator"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ventas" model="ir.actions.act_window">
        <field name="name">Ventas</field>
        <field name="res_model">iv.venta</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
